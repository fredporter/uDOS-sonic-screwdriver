# Examples

`examples/` is the smallest inspectable walkthrough lane for Sonic-owned
deployment flows.

Current example lanes:

- `basic-sonic-session.md` for the shortest local packaging workflow
- `custom-build-engine/` for extended build-engine experimentation

Phase 1 rule:

- keep examples operator-readable and deployment-scoped
- route family-wide teaching into `uDOS-docs` and `uDOS-dev`
