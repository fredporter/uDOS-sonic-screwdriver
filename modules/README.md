# Sonic Modules

`modules/` contains the install and deployment lanes that Sonic owns.

Current active module concepts:

- USB installer
- dual-boot deployment
- rescue and recovery
- `uHOME` bundle boundary marker

`uHOME` bundle contracts are not a local Sonic module anymore. They are owned
externally by `uHOME-server`.
